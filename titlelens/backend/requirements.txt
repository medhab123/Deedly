# TitleLens Backend — Python 3.10+
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0
python-dotenv>=1.0.0

# AI — use one of these
openai>=1.10.0

# ML prediction layer (regression + ensemble)
scikit-learn>=1.3.0
numpy>=1.24.0
# Optional: add XGBoost to the ensemble
# xgboost>=2.0.0

# Property Behavior Network (graph + Node2Vec + anomaly)
networkx>=3.0
node2vec>=0.5.0
